---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - tailwind.config.ts
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - .eslintrc.json
  - .gitignore
autonomous: true
requirements:
  - TECH-01
  - TECH-02

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Next.js dev server starts without errors on localhost:3000"
    - "App Router is active (app/ directory with layout.tsx and page.tsx)"
    - "TypeScript compiles without errors"
    - "Tailwind CSS processes utility classes"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15+ with App Router, React 19, TypeScript, Tailwind CSS 4"
      contains: "next"
    - path: "app/layout.tsx"
      provides: "Root layout with html/body structure"
    - path: "app/page.tsx"
      provides: "Home page component"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
    - path: "tailwind.config.ts"
      provides: "Tailwind CSS configuration"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import in layout root"
      pattern: "globals.css"
---

<objective>
Initialize the Next.js project scaffold with App Router, TypeScript, Tailwind CSS, and clean boilerplate.

Purpose: Establish the foundation every subsequent plan builds on — framework, build pipeline, dev server.
Output: Working Next.js app that boots, compiles, and serves pages.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create Next.js app and verify it boots</name>
  <files>package.json, next.config.ts, tsconfig.json, tailwind.config.ts, postcss.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, .eslintrc.json, .gitignore</files>
  <read_first>
    - Nothing — greenfield project, no existing code to read
  </read_first>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --turbopack` in the project root `D:\Code\personal-web-portofolio-v2`. Use `.` to init in current directory (the folder already has .git and .planning). If prompted about existing files, confirm proceed. This creates: package.json with next/react/tailwind/typescript deps, app/layout.tsx, app/page.tsx, app/globals.css, next.config.ts, tsconfig.json, tailwind.config.ts, postcss.config.mjs, .eslintrc.json. After creation, run `npm run dev` and verify it starts on localhost:3000 without errors. Then stop the dev server. This is the tracer — it proves the full build pipeline works end-to-end: framework boots, TypeScript compiles, Tailwind processes, dev server serves.
  </action>
  <verify>
    <automated>cd D:\Code\personal-web-portofolio-v2 && npm run build 2>&1 | Select-String -Pattern "Compiled successfully|Route \(app\)" | Measure-Object | Select-Object -ExpandProperty Count</automated>
    Build output must show at least 1 match confirming successful compilation. Also verify: `Test-Path app/layout.tsx`, `Test-Path app/page.tsx`, `Test-Path package.json`.
  </verify>
  <acceptance_criteria>
    - `npm run build` completes without errors
    - app/layout.tsx, app/page.tsx, app/globals.css exist
    - package.json contains next, react, react-dom, typescript, tailwindcss
    - Dev server starts on localhost:3000 (verified during task, stopped after)
  </acceptance_criteria>
  <done>Next.js app created, builds successfully, dev server boots cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Clean boilerplate and set up minimal pixel-themed page</name>
  <files>app/layout.tsx, app/page.tsx, app/globals.css</files>
  <read_first>
    - app/layout.tsx (created by create-next-app)
    - app/page.tsx (created by create-next-app)
    - app/globals.css (created by create-next-app)
  </read_first>
  <action>
    Strip the default Next.js boilerplate content. In app/layout.tsx: remove the default className on body (keep html lang="en"), remove the default Geist font imports and CSS variable setup, keep the metadata export but set title to "Tody Prasetya | Backend Engineer" and description to "Pixel-art portfolio of Tody Prasetya, Backend/Full Stack Engineer". In app/globals.css: remove all default Tailwind imports and custom properties, replace with just the three Tailwind directives (@tailwind base; @tailwind components; @tailwind utilities;) plus a minimal body reset (margin: 0, background-color: #1a1a2e which is a dark retro blue). In app/page.tsx: replace the default content with a simple centered div showing "LOADING..." text using Tailwind classes (text-white, text-center, min-h-screen, flex, items-center, justify-center) — this placeholder will be replaced by the base layout in Plan 04. Remove any unused default assets (public/next.svg, public/file.svg, public/globe.svg, public/window.svg, public/vercel.svg if they exist).
  </action>
  <verify>
    <automated>cd D:\Code\personal-web-portofolio-v2 && npm run build 2>&1 | Select-String -Pattern "Compiled successfully|Route \(app\)" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx renders a centered "LOADING..." placeholder
    - app/layout.tsx has metadata with title "Tody Prasetya | Backend Engineer"
    - app/globals.css contains only Tailwind directives and body background reset
    - No default Next.js SVG assets remain in public/
    - `npm run build` still succeeds after cleanup
  </acceptance_criteria>
  <done>Boilerplate cleaned, minimal pixel-themed placeholder page renders, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry | Package dependencies pulled from public registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | npm install | low | accept | create-next-app is the official Next.js scaffold; supply-chain risk is standard for any npm project. No elevated mitigation needed for initial scaffold. |
</threat_model>

<verification>
- `npm run dev` starts without errors on localhost:3000
- `npm run build` completes successfully
- No TypeScript errors in terminal output
- app/ directory contains layout.tsx, page.tsx, globals.css
</verification>

<success_criteria>
- Next.js 15+ project initialized with App Router
- TypeScript, Tailwind CSS, ESLint configured
- Dev server runs without errors
- Clean boilerplate with pixel-themed dark background
- Build passes cleanly
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
