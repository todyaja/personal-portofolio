---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - app/favicon.ico
autonomous: true
requirements:
  - VIS-01

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Root layout wraps all pages in a pixel-art styled container"
    - "Pixel fonts applied globally via layout"
    - "Dark retro background visible on every page"
    - "Metadata renders correct title, description, and OG tags"
    - "Favicon displays in browser tab"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root layout with pixel container, font variables, metadata"
      contains: "pixel-bg-dark"
    - path: "app/page.tsx"
      provides: "Home page with pixel-styled hero content"
    - path: "app/globals.css"
      provides: "Global pixel body styles and container layout"
    - path: "app/favicon.ico"
      provides: "Browser tab favicon"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css' in layout"
      pattern: "globals.css"
    - from: "app/layout.tsx"
      to: "public/assets/branding/favicon_animated.gif"
      via: "favicon configured in metadata or placed at app/favicon.ico"
      pattern: "favicon"
---

<objective>
Create the root layout with pixel-art styled container and configure metadata and favicon.

Purpose: This is the shell every page renders inside. It applies the pixel aesthetic globally and sets up SEO metadata. Phase 2+ builds navigation and content inside this layout.
Output: Root layout with pixel container, metadata, and favicon — the finished foundation.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build root layout with pixel-art container</name>
  <files>app/layout.tsx, app/globals.css, app/page.tsx</files>
  <read_first>
    - app/layout.tsx (from Plan 01/02 — has font imports, metadata, html/body structure)
    - app/globals.css (from Plan 02 — has color variables, pixel utility classes)
    - app/page.tsx (from Plan 02 — has pixel-styled placeholder content)
  </read_first>
  <action>
    **app/globals.css:** Add global body styles after the existing CSS variables and utility classes. Set `body` to: `background-color: var(--pixel-bg-dark)`, `color: var(--pixel-text-primary)`, `font-family: var(--font-pixel-body)`, `min-height: 100vh`, `image-rendering: pixelated` (ensures any pixel art images stay crisp, not anti-aliased). Add a `.pixel-container` class: `max-width: 960px`, `margin: 0 auto`, `padding: 2rem`, `min-height: 100vh`. Add a `.pixel-scanline` overlay class for subtle CRT effect: `pointer-events: none`, `position: fixed`, `inset: 0`, `background: repeating-linear-gradient(0deg, rgba(0,0,0,0.1) 0px, rgba(0,0,0,0.1) 1px, transparent 1px, transparent 3px)`, `z-index: 9999`.

    **app/layout.tsx:** Wrap the `{children}` in a div with className `pixel-container`. Add a div with className `pixel-scanline` as a sibling (after children) for the CRT overlay effect. Ensure the html element has `className` combining both font variables from Plan 02 (`pressStart2P.variable` and `vt323.variable`). Add `suppressHydrationWarning` to the html element (required by next/font). The body should have `className="antialiased"` plus any global body classes.

    **app/page.tsx:** Update the page to be a proper pixel-art styled landing. Use `.pixel-panel` for the main content container. Inside, render: an h1 with `.pixel-heading` showing "TODY PRASETYA", a p with `.pixel-body-text` showing "Backend / Full Stack Engineer", and a decorative pixel divider (an hr styled with `border: none`, `border-top: 2px dashed var(--pixel-border)`, `margin: 1.5rem 0`). This is the foundation page — Phase 2 will add navigation and hero section around it.
  </action>
  <verify>
    <automated>cd D:\Code\personal-web-portofolio-v2 && npm run build 2>&1 | Select-String -Pattern "Compiled successfully" | Measure-Object | Select-Object -ExpandProperty Count</automated>
    Also verify: `Select-String -Pattern "pixel-container" app/layout.tsx` returns matches, `Select-String -Pattern "pixel-scanline" app/globals.css` returns matches, `Select-String -Pattern "image-rendering.*pixelated" app/globals.css` returns matches.
  </verify>
  <acceptance_criteria>
    - Root layout wraps children in .pixel-container div
    - CRT scanline overlay div present in layout
    - Body has pixel-bg-dark background, pixel-body font, image-rendering: pixelated
    - .pixel-container centers content at max-width 960px
    - app/page.tsx renders pixel-panel with name, title, and dashed divider
    - html element has both font variable classes and suppressHydrationWarning
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Root layout renders pixel-art container with scanline overlay, page shows pixel-styled name and title</done>
</task>

<task type="auto">
  <name>Task 2: Configure metadata and favicon</name>
  <files>app/layout.tsx, app/favicon.ico</files>
  <read_first>
    - app/layout.tsx (just modified in Task 1 — has metadata export)
    - public/assets/branding/favicon_animated.gif (copied in Plan 03)
  </read_first>
  <action>
    **Metadata:** In app/layout.tsx, update the metadata export to include full SEO setup: `title: { default: "Tody Prasetya | Backend Engineer", template: "%s | Tody Prasetya" }`, `description: "Pixel-art portfolio of Tody Prasetya — Backend/Full Stack Engineer specializing in Go, JavaScript, Java, TypeScript, PostgreSQL, SQL Server"`, `openGraph: { title: "Tody Prasetya | Backend Engineer", description: "Pixel-art portfolio — Backend/Full Stack Engineer", type: "website", locale: "en_US" }`, `twitter: { card: "summary_large_image", title: "Tody Prasetya | Backend Engineer", description: "Pixel-art portfolio — Backend/Full Stack Engineer" }`.

    **Favicon:** Next.js App Router handles favicons via files in the app/ directory. Copy the animated gif favicon from `public/assets/branding/favicon_animated.gif` to `app/favicon.ico` — but since .gif cannot be .ico, instead create `app/icon.png` by copying `public/assets/branding/t_logo.png` to `app/icon.png` (Next.js auto-discovers app/icon.png as the default icon). Also create `app/apple-icon.png` as another copy of t_logo.png for Apple touch icon. Next.js will auto-generate the favicon link tags from these files.

    Alternatively, if you prefer the animated gif as favicon, place it at `public/favicon.ico` — but browsers may not animate it. The static t_logo.png approach is more reliable.
  </action>
  <verify>
    <automated>cd D:\Code\personal-web-portofolio-v2 && npm run build 2>&1 | Select-String -Pattern "Compiled successfully|Route \(app\)" | Measure-Object | Select-Object -ExpandProperty Count</automated>
    Also verify: `Test-Path "D:\Code\personal-web-portofolio-v2\app\icon.png"`, `Test-Path "D:\Code\personal-web-portofolio-v2\app\apple-icon.png"`, `Select-String -Pattern "openGraph" app/layout.tsx` returns matches.
  </verify>
  <acceptance_criteria>
    - Metadata has title, description, openGraph, and twitter card config
    - Title uses template pattern for page-specific titles
    - app/icon.png exists (copied from t_logo.png)
    - app/apple-icon.png exists (copied from t_logo.png)
    - `npm run build` succeeds and generates icon routes
    - Description mentions key technologies and pixel-art theme
  </acceptance_criteria>
  <done>Full metadata configured, favicon and apple-icon set from personal logo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML output | Metadata renders into HTML head |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Metadata | low | accept | Metadata contains only public professional information (name, title, tech stack). No sensitive data exposed. |
</threat_model>

<verification>
- Dev server renders pixel-styled page with dark background, pixel fonts, scanline overlay
- View source shows correct meta tags (title, description, OG, Twitter)
- Favicon displays in browser tab
- Layout container centers content at 960px max-width
- All pixel utility classes render correctly on the page
- `npm run build` produces production build without errors
</verification>

<success_criteria>
- Root layout wraps all pages in pixel-art styled container with scanline overlay
- Pixel fonts applied globally (Press Start 2P for headings, VT323 for body)
- Dark retro background (#1a1a2e) on every page
- Metadata renders correct title, description, OG tags, Twitter card
- Favicon and Apple touch icon display personal logo
- Phase 1 success criteria fully met: dev server runs, fonts load, assets accessible, layout renders with pixel styling
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
