---
phase: 02-layout-navigation
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - components/HeroSection.tsx
  - app/page.tsx
  - app/globals.css
autonomous: true
requirements:
  - SEC-01
  - VIS-05

estimate:
  tokens: 45000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - Hero section displays pixel avatar, name, title, and intro text
    - Hero section is the first thing visible below navigation
    - Layout reads as an RPG dialogue/introduction screen
    - Hero section is responsive (stacks vertically on mobile)
  artifacts:
    - components/HeroSection.tsx exists
    - app/page.tsx renders HeroSection as main content
  key_links:
    - HeroSection imports and uses PixelAvatar component
    - page.tsx renders HeroSection below Navigation (from layout)
---

<objective>
Build the hero/intro section that combines the pixel avatar, name, title, and a brief introduction into a cohesive RPG-styled landing panel.

Purpose: The hero section is the first impression — it must immediately communicate who Tody is and set the pixel-art tone for the entire portfolio.
Output: HeroSection component integrated as the main page content.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/page.tsx
@app/globals.css
@components/PixelAvatar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection component</name>
  <files>components/HeroSection.tsx</files>
  <action>
Create a server component at `components/HeroSection.tsx` that renders the hero/intro section.

Structure:
- Outer wrapper: `<section>` with `id="hero"` and className `pixel-hero`
- Layout: Flexbox, centered content, responsive (column on mobile, row on desktop)
- Left side (or top on mobile): `<PixelAvatar size="lg" animated={true} />`
- Right side (or bottom on mobile): Content panel with:
  - Name: `<h1>` with `pixel-heading` class, text "TODY PRASETYA", color `--pixel-highlight`
  - Title: `<p>` with `pixel-body-text` class, text "Backend / Full Stack Engineer"
  - Divider: `<hr>` with pixel dashed style (reuse the dashed border pattern from existing page.tsx)
  - Intro text: `<p>` with `pixel-body-text` class, brief 2-3 sentence introduction:
    "Welcome to my pixel-art portfolio. I build robust backend systems and full-stack applications with Go, JavaScript, Java, TypeScript, and PostgreSQL. Let's create something awesome together."
  - Optional: A pixel-styled call-to-action button or link (e.g., "VIEW MY WORK" linking to `/#experience`)

Styling:
- The section should feel like an RPG dialogue box or character introduction screen
- Use `pixel-panel` class for the content area
- Avatar and content panel should have visual balance
- On mobile: avatar centered above text
- On desktop (min-width: 768px): avatar left, content right, side by side

Import PixelAvatar from `@/components/PixelAvatar`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Open browser, verify hero section renders with avatar, name, title, intro text. Check mobile layout stacks vertically.</manual>
  </verify>
  <done>HeroSection renders with avatar, name, title, intro text, responsive layout works</done>
</task>

<task type="auto">
  <name>Task 2: Integrate HeroSection into page.tsx and add hero CSS</name>
  <files>app/page.tsx, app/globals.css</files>
  <action>
Update `app/page.tsx` to render the HeroSection component as the main content.

Changes to page.tsx:
- Remove the existing placeholder content (the pixel-panel with name/title)
- Import `HeroSection` from `@/components/HeroSection`
- Render `<HeroSection />` as the main content
- The page should now be: `<main><HeroSection /></main>` or just `<HeroSection />`
- Remove the old `<div className="min-h-screen flex items-center justify-center">` wrapper

Changes to globals.css:
- Add `.pixel-hero` class:
  - `display: flex`
  - `flex-direction: column`
  - `align-items: center`
  - `gap: 2rem`
  - `padding: 2rem 0`
  - `@media (min-width: 768px)`: `flex-direction: row`, `align-items: flex-start`, `gap: 3rem`
- Add `.pixel-hero-content` class:
  - `flex: 1`
  - `text-align: center` on mobile
  - `@media (min-width: 768px)`: `text-align: left`
- Add `.pixel-hero-avatar` class (wrapper for avatar in hero):
  - `flex-shrink: 0`
  - `display: flex`
  - `justify-content: center`

Verify build succeeds and page renders correctly.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>page.tsx renders HeroSection, old placeholder removed, hero CSS classes exist</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static content rendering | No user input processed in hero section |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Hero text content | low | accept | Public portfolio content, intentionally shared |
</threat_model>

<verification>
1. `npm run build` succeeds
2. Hero section renders at top of page content (below navigation)
3. Pixel avatar displays with animated idle bob
4. Name "TODY PRASETYA" displays in pixel-heading with highlight color
5. Title "Backend / Full Stack Engineer" displays below name
6. Intro paragraph renders with pixel-body-text styling
7. Mobile layout: avatar centered above text
8. Desktop layout: avatar left, content right
9. Section has id="hero" for anchor linking
</verification>

<success_criteria>
- HeroSection component exists at components/HeroSection.tsx
- page.tsx renders HeroSection as main content
- Old placeholder content removed from page.tsx
- Hero section displays avatar, name, title, intro text
- Responsive layout works (column on mobile, row on desktop)
- Section styled with pixel-panel and pixel-art aesthetic
- Section has id for anchor navigation from nav links
</success_criteria>

<output>
Create `.planning/phases/02-layout-navigation/02-04-SUMMARY.md` when done
</output>
