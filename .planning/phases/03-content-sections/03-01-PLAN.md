---
phase: 03-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ExperienceTimeline.tsx
  - app/globals.css
autonomous: true
requirements:
  - SEC-02
  - SEC-03

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - All 5 experience roles render with company, role, duration, description, and tech stack
    - Each entry is visually distinct with pixel-art RPG dialogue box styling
    - Timeline is readable on mobile (single column) and desktop
  artifacts:
    - components/ExperienceTimeline.tsx
  key_links:
    - Section id="experience" matches nav link href="/#experience"
---

<objective>
Build the Experience Timeline component displaying all 5 roles in pixel-art RPG quest-log style.

Purpose: This is the core content section — recruiters and visitors need to see Tody's career progression at a glance. Each entry must show company, role, duration, bullet-point descriptions, and tech stack used.
Output: ExperienceTimeline component + timeline CSS classes appended to globals.css
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/globals.css
@components/HeroSection.tsx
@components/PixelAvatar.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create ExperienceTimeline component with all 5 roles</name>
  <files>components/ExperienceTimeline.tsx</files>
  <action>
    Create a server component at components/ExperienceTimeline.tsx that renders all 5 experience entries.

    Structure:
    - Outer wrapper: section with id="experience" and className="pixel-experience"
    - Section heading: h2 with className="pixel-heading" text "EXPERIENCE" styled like an RPG quest log title
    - Each role rendered as a pixel-panel card with className="pixel-timeline-entry"

    Data — embed directly as a const array of 5 objects (no external data file needed for 5 static entries):

    1. IT Developer (Full Stack Engineer) | CIMB NIAGA | Tangerang | 02/2025 - Present
       Bullets: Designed and implemented new features for the OCTO Connect project; Collaborated cross-functionally with multiple teams; Developed comprehensive unit tests and engineering specifications
       Tech: Go, Java, NextJS, Redis, PostgreSQL, BitBucket, Jira, Confluence, Red Hat OpenShift

    2. Lead Backend Engineer | Vokraf | Jakarta | 09/2024 - 12/2024
       Bullets: Collaborate effectively with cross-functional teams for strategic business decision-making; Provide leadership and guidance on code architecture; Conduct thorough reviews of backend code authored by peers
       Tech: (not specified in CV — omit tech row or show "Various")

    3. Backend Engineer | Vokraf | Jakarta | 04/2024 - 09/2024
       Bullets: Revamped the existing API into a microservice architecture; Documented API specifications and microservice interactions using ClickUp; Added unit testing for all microservices using Jest
       Tech: NestJS, Typescript, Drizzle-ORM, PostgreSQL, Unit Testing, Jest, Kafka, RabbitMQ, Docker, ClickUp, GitHub

    4. Full Stack Engineer | BINUS GROUP | Jakarta | 02/2022 - 02/2024
       Bullets: Develop, Enhance, and Maintain multiple learning management system projects; Help external teams configure, deploy, and implement changes from dev to production
       Tech: .NET Core, React JS, Cosmos DB, Azure Function, Azure DevOps, CodeIgniter, SQL Server, Java, Kotlin

    5. Backend Engineer | Bali Cipta Innovator | Jakarta | 02/2021 - 08/2021
       Bullets: Design and implemented Restful API, improving data accessibility and integration; Implemented schedulers from the database to automatically insert daily production
       Tech: .NET Entity Framework, Oracle DB, Gitlab

    Each entry layout (inside pixel-panel):
    - Top row: Company name (pixel-heading, --pixel-highlight color) + Duration (pixel-body-text, --pixel-text-secondary) on the right
    - Second row: Role/title (pixel-body-text, bold or --pixel-highlight)
    - Location line: small text with --pixel-text-secondary
    - Bullet list: ul/li with pixel-body-text class, each bullet from the data above
    - Tech stack row: flex-wrap container with small pixel-bordered badges for each tech (className="pixel-tech-badge"). Use existing tech logo images where available via next/image: java.png, kotlin.png, net.png, oracle.png, postgre.png, reactjs.png. For techs without logos (Go, Redis, NestJS, Docker, etc.), render text-only badges.
    - Add a vertical pixel-border line connecting entries on desktop (left side), hidden on mobile

    Use existing utility classes: pixel-panel, pixel-border, pixel-heading, pixel-body-text. Do NOT create new global CSS classes in this task — use inline styles for layout specifics (following HeroSection pattern).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - components/ExperienceTimeline.tsx exists and exports default component
    - All 5 roles render with company, role, duration, location, bullets, and tech stack
    - Tech logos display for java, kotlin, .net, oracle, postgresql, reactjs where matching
    - Text-only badges for techs without logos
    - Section has id="experience"
    - Uses pixel-panel, pixel-border, pixel-heading, pixel-body-text classes
    - npm run build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add timeline CSS classes to globals.css</name>
  <files>app/globals.css</files>
  <action>
    Append a labeled CSS section to the END of app/globals.css (after the existing Hero Section section). Follow the existing comment pattern: /* ── Experience Timeline ── */.

    Classes to add:
    - .pixel-experience: section wrapper, padding-top 2rem, margin-bottom 2rem
    - .pixel-timeline-entry: margin-bottom 1.5rem, position relative (for connector line)
    - .pixel-timeline-header: flex layout, space-between, align-items baseline, flex-wrap wrap for mobile
    - .pixel-timeline-company: pixel-heading font, font-size 0.75rem, color --pixel-highlight
    - .pixel-timeline-duration: pixel-body-text, font-size 0.625rem, color --pixel-text-secondary
    - .pixel-timeline-role: pixel-body-text, font-weight bold, margin 0.5rem 0
    - .pixel-timeline-location: pixel-body-text, font-size 0.625rem, color --pixel-text-secondary, margin-bottom 0.75rem
    - .pixel-timeline-bullets: list-style none, padding-left 0, margin 0.75rem 0
    - .pixel-timeline-bullets li: pixel-body-text, font-size 0.625rem, padding-left 1rem, position relative, margin-bottom 0.5rem. Add a pixel-style bullet via ::before (small square, not a circle — use content "►" or a 4px square with background --pixel-border)
    - .pixel-tech-badges: display flex, flex-wrap wrap, gap 0.5rem, margin-top 0.75rem
    - .pixel-tech-badge: display inline-flex, align-items center, gap 0.25rem, padding 0.25rem 0.5rem, font-size 0.5rem, font-family pixel-heading, background --pixel-accent, border 1px solid --pixel-border, color --pixel-text-primary
    - .pixel-tech-badge img: width 16px, height 16px, image-rendering pixelated
    - Desktop connector line: @media (min-width: 768px) .pixel-timeline-entry::before — 2px solid --pixel-border line on the left side, connecting entries vertically. Add a small pixel dot (6px square) at the top of each entry.
    - Responsive: on mobile, hide the connector line, full-width entries
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - globals.css contains labeled /* ── Experience Timeline ── */ section
    - All timeline CSS classes defined
    - Desktop connector line visible at 768px+
    - Mobile: no connector line, full-width entries
    - npm run build succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CV data → component | Static embedded data, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | Experience data | low | accept | All data is hardcoded static content in the component. No user input or API calls. CV data cannot be tampered with at runtime. |
</threat_model>

<verification>
- npm run build succeeds with no errors
- ExperienceTimeline component renders all 5 entries
- Each entry shows: company, role, duration, location, bullets, tech stack
- Tech logos display correctly for technologies with available assets
- Section has id="experience" matching navigation link
</verification>

<success_criteria>
- All 5 experience entries display with complete data
- Pixel-art RPG styling consistent with existing design system
- Responsive on mobile and desktop
- Tech badges show logos where assets exist, text-only otherwise
</success_criteria>

<output>
Create `.planning/phases/03-content-sections/03-01-SUMMARY.md` when done
</output>
