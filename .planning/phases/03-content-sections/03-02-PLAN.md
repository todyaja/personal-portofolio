---
phase: 03-content-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/SkillsSection.tsx
  - app/globals.css
autonomous: true
requirements:
  - SEC-04
  - AST-05

estimate:
  tokens: 35000
  raw_tokens: 23000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - Skills section displays all core technologies: Go, JavaScript, Java, TypeScript, PostgreSQL, SQL Server
    - Additional tech from experience entries also shown (NestJS, Docker, Kafka, .NET, React, etc.)
    - Tech logos render for technologies with available asset images
  artifacts:
    - components/SkillsSection.tsx
  key_links:
    - Section id="skills" matches nav link href="/#skills"
---

<objective>
Build the Skills Section displaying all technologies in pixel-art RPG inventory-screen style with tech logos.

Purpose: Visitors need to quickly assess Tody's technical capabilities. The skills section should group technologies logically and use the available tech logo assets for visual impact.
Output: SkillsSection component + skills CSS classes appended to globals.css
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/globals.css
@components/HeroSection.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create SkillsSection component with tech categories and logos</name>
  <files>components/SkillsSection.tsx</files>
  <action>
    Create a server component at components/SkillsSection.tsx.

    Structure:
    - Outer wrapper: section with id="skills" and className="pixel-skills"
    - Section heading: h2 with className="pixel-heading" text "SKILLS" styled like an RPG inventory title
    - Content: pixel-panel wrapper containing categorized skill groups

    Data — embed as const arrays grouped by category:

    Category 1: "LANGUAGES"
    - Go (no logo asset available — text badge)
    - JavaScript (no logo asset — text badge)
    - Java (logo: /assets/tech/java.png)
    - TypeScript (no logo — text badge)

    Category 2: "DATABASES"
    - PostgreSQL (logo: /assets/tech/postgre.png)
    - SQL Server (no logo — text badge)
    - Oracle DB (logo: /assets/tech/oracle.png)
    - Cosmos DB (no logo — text badge)

    Category 3: "FRAMEWORKS & TOOLS"
    - NextJS / React (logo: /assets/tech/reactjs.png)
    - .NET Core (logo: /assets/tech/net.png)
    - NestJS (no logo — text badge)
    - CodeIgniter (no logo — text badge)
    - Kotlin (logo: /assets/tech/kotlin.png)
    - PHP (logo: /assets/tech/php.png)

    Category 4: "INFRASTRUCTURE & MESSAGING"
    - Docker (no logo — text badge)
    - Kafka (no logo — text badge)
    - RabbitMQ (no logo — text badge)
    - Redis (no logo — text badge)
    - Azure Function (no logo — text badge)
    - Azure DevOps (no logo — text badge)
    - Red Hat OpenShift (no logo — text badge)

    Category 5: "TOOLS & PLATFORMS"
    - Git / GitHub / BitBucket / Gitlab (no logos — text badges)
    - Jira / Confluence / ClickUp (no logos — text badges)
    - Jest (no logo — text badge)
    - Drizzle-ORM (no logo — text badge)

    Each category rendered as:
    - Category heading: h3 with className="pixel-heading" and className="pixel-skill-category-title", smaller font (0.625rem)
    - Grid of skill items using className="pixel-skill-grid" (flex-wrap layout)
    - Each skill item: div with className="pixel-skill-item" containing:
      - If logo available: next/image with the logo (32x32px, image-rendering pixelated) + tech name below
      - If no logo: text-only with a pixel-art icon prefix (use a unicode character like ◆ or ■ for visual consistency)

    Use existing utility classes: pixel-panel, pixel-border, pixel-heading, pixel-body-text.
    Use inline styles for layout specifics following the HeroSection pattern.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - components/SkillsSection.tsx exists and exports default component
    - All core skills displayed: Go, JavaScript, Java, TypeScript, PostgreSQL, SQL Server
    - Additional tech from experience also shown
    - Tech logos render for java, kotlin, net, oracle, postgre, reactjs, php
    - Text badges for techs without logos
    - Section has id="skills"
    - npm run build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add skills CSS classes to globals.css</name>
  <files>app/globals.css</files>
  <action>
    Append a labeled CSS section to the END of app/globals.css. Follow the existing comment pattern: /* ── Skills Section ── */.

    Classes to add:
    - .pixel-skills: section wrapper, padding-top 2rem, margin-bottom 2rem
    - .pixel-skill-category: margin-bottom 1.5rem
    - .pixel-skill-category-title: pixel-heading font, font-size 0.625rem, color --pixel-highlight, margin-bottom 0.75rem, border-bottom 1px solid --pixel-border, padding-bottom 0.5rem
    - .pixel-skill-grid: display flex, flex-wrap wrap, gap 0.75rem
    - .pixel-skill-item: display flex, flex-direction column, align-items center, gap 0.25rem, padding 0.5rem, min-width 80px, background --pixel-accent, border 2px solid --pixel-border, box-shadow 2px 2px 0px rgba(0,0,0,0.5), transition transform 0.1s. On hover: transform translate(-1px, -1px), box-shadow 3px 3px 0px rgba(0,0,0,0.5)
    - .pixel-skill-item img: width 32px, height 32px, image-rendering pixelated, object-fit contain
    - .pixel-skill-item span: pixel-heading font, font-size 0.5rem, color --pixel-text-primary, text-align center, text-transform uppercase
    - .pixel-skill-item.text-only: for items without logos, add a ::before pseudo-element with a pixel character (◆) in --pixel-highlight color, font-size 1rem
    - Responsive: skill items wrap naturally, no specific breakpoint needed since flex-wrap handles it
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - globals.css contains labeled /* ── Skills Section ── */ section
    - All skill CSS classes defined
    - Skill items have hover effect (pixel-style translate)
    - Text-only items show pixel character prefix
    - npm run build succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skills data → component | Static embedded data, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | Skills data | low | accept | All skill data is hardcoded static content. No user input or API calls involved. |
</threat_model>

<verification>
- npm run build succeeds with no errors
- SkillsSection renders all skill categories
- Tech logos display for java, kotlin, net, oracle, postgre, reactjs, php
- Text badges render for technologies without logos
- Section has id="skills" matching navigation link
</verification>

<success_criteria>
- All core skills displayed: Go, JavaScript, Java, TypeScript, PostgreSQL, SQL Server
- Additional tech from experience also shown
- Pixel-art RPG inventory styling consistent with design system
- Tech logos render correctly where assets exist
- Responsive flex-wrap layout works on all screen sizes
</success_criteria>

<output>
Create `.planning/phases/03-content-sections/03-02-SUMMARY.md` when done
</output>
