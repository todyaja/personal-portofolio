---
phase: 03-content-sections
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/EducationSection.tsx
  - app/globals.css
autonomous: true
requirements:
  - SEC-05
  - AST-04

estimate:
  tokens: 25000
  raw_tokens: 17000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - Education section displays Computer Science degree from Bina Nusantara University
    - Cumlaude honor and 3.87 GPA shown prominently
    - Binus logo renders from /assets/logos/binus_univ.png
  artifacts:
    - components/EducationSection.tsx
  key_links:
    - Binus logo image loads from /assets/logos/binus_univ.png
---

<objective>
Build the Education Section displaying Tody's CS degree from Binus with pixel-art RPG diploma/certificate styling.

Purpose: Education is a key credential — the Cumlaude distinction and high GPA should be prominently displayed. The Binus logo adds visual authenticity.
Output: EducationSection component + education CSS classes appended to globals.css
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/globals.css
@components/HeroSection.tsx
@components/PixelAvatar.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create EducationSection component with Binus logo and degree info</name>
  <files>components/EducationSection.tsx</files>
  <action>
    Create a server component at components/EducationSection.tsx.

    Structure:
    - Outer wrapper: section with className="pixel-education"
    - Section heading: h2 with className="pixel-heading" text "EDUCATION" styled like an RPG certificate/achievement title
    - Content: pixel-panel card with className="pixel-education-card"

    Card layout (flex row on desktop, column on mobile):
    - Left/top: Binus logo using next/image, src="/assets/logos/binus_univ.png", alt="Bina Nusantara University logo", width 96, height 96. Wrap in a div with className="pixel-education-logo" and pixel-border class.
    - Right/bottom: Text content div with className="pixel-education-content"
      - Degree: "Computer Science" — pixel-heading, --pixel-highlight color, font-size 0.75rem
      - University: "Bina Nusantara University" — pixel-body-text, font-size 0.625rem
      - Honor: "Cumlaude" — pixel-body-text, --pixel-success color, with a small pixel star prefix (★ or ◆)
      - GPA: "GPA: 3.87 / 4.00" — pixel-body-text, --pixel-text-secondary, font-size 0.625rem

    Style the card like an RPG achievement certificate — the panel should feel like a diploma or quest completion reward. Use pixel-panel + pixel-border for the frame.

    Use existing utility classes: pixel-panel, pixel-border, pixel-heading, pixel-body-text.
    Use inline styles for layout specifics following HeroSection pattern.

  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - components/EducationSection.tsx exists and exports default component
    - Binus logo renders from /assets/logos/binus_univ.png with pixel-border frame
    - Degree "Computer Science" displayed prominently
    - "Bina Nusantara University" shown
    - "Cumlaude" honor displayed in --pixel-success color
    - "GPA: 3.87 / 4.00" shown
    - Responsive layout: column on mobile, row on desktop
    - npm run build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add education CSS classes to globals.css</name>
  <files>app/globals.css</files>
  <action>
    Append a labeled CSS section to the END of app/globals.css. Follow the existing comment pattern: /* ── Education Section ── */.

    Classes to add:
    - .pixel-education: section wrapper, padding-top 2rem, margin-bottom 2rem
    - .pixel-education-card: display flex, flex-direction column, align-items center, gap 1.5rem, text-align center. At 768px+: flex-direction row, align-items center, gap 2rem, text-align left
    - .pixel-education-logo: flex-shrink 0, width 96px, height 96px, display flex, align-items center, justify-content center, background --pixel-bg-dark, padding 0.5rem. Image inside: width 100%, height 100%, object-fit contain, image-rendering pixelated
    - .pixel-education-content: flex 1
    - .pixel-education-degree: pixel-heading font, font-size 0.75rem, color --pixel-highlight, margin-bottom 0.5rem
    - .pixel-education-university: pixel-body-text, font-size 0.625rem, color --pixel-text-primary, margin-bottom 0.5rem
    - .pixel-education-honor: pixel-body-text, font-size 0.625rem, color --pixel-success, margin-bottom 0.25rem. Add a ::before with "★ " in --pixel-highlight
    - .pixel-education-gpa: pixel-body-text, font-size 0.625rem, color --pixel-text-secondary

  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - globals.css contains labeled /* ── Education Section ── */ section
    - All education CSS classes defined
    - Responsive layout: column on mobile, row on desktop
    - Binus logo styled with pixel-art frame
    - npm run build succeeds
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                             |
| -------------------------- | --------------------------------------- |
| Education data → component | Static embedded data, no external input |

## STRIDE Threat Register

| Threat ID | Category  | Component      | Severity | Disposition | Mitigation Plan                                                             |
| --------- | --------- | -------------- | -------- | ----------- | --------------------------------------------------------------------------- |
| T-03-03   | Tampering | Education data | low      | accept      | All education data is hardcoded static content. No user input or API calls. |

</threat_model>

<verification>
- npm run build succeeds with no errors
- EducationSection renders with Binus logo, degree, university, honor, GPA
- Binus logo loads from /assets/logos/binus_univ.png
- Responsive layout works on mobile and desktop
</verification>

<success_criteria>

- Education section displays all required information
- Binus logo renders correctly with pixel-art frame
- Cumlaude and GPA prominently shown
- Pixel-art RPG certificate styling consistent with design system
- Responsive on mobile and desktop
  </success_criteria>

<output>
Create `.planning/phases/03-content-sections/03-03-SUMMARY.md` when done
</output>
