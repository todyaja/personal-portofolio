---
phase: 03-content-sections
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ContactSection.tsx
  - app/globals.css
autonomous: true
requirements:
  - SEC-06

estimate:
  tokens: 25000
  raw_tokens: 17000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - Contact section displays email, phone, and location
    - Email link opens mailto: client
    - Phone link opens tel: client
    - All links are keyboard-navigable and accessible
  artifacts:
    - components/ContactSection.tsx
  key_links:
    - Section id="contact" matches nav link href="/#contact"
    - mailto:todyprasetya@gmail.com link works
    - tel:08118422002 link works
---

<objective>
Build the Contact Section with pixel-art RPG message-box styling displaying email, phone, and location with working links.

Purpose: Visitors need a way to reach Tody. The contact section should make it trivially easy to email or call, while maintaining the pixel-art aesthetic.
Output: ContactSection component + contact CSS classes appended to globals.css
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/globals.css
@components/HeroSection.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create ContactSection component with pixel-art styled contact info</name>
  <files>components/ContactSection.tsx</files>
  <action>
    Create a server component at components/ContactSection.tsx.

    Structure:
    - Outer wrapper: section with id="contact" and className="pixel-contact"
    - Section heading: h2 with className="pixel-heading" text "CONTACT" styled like an RPG message/quest board title
    - Content: pixel-panel card with className="pixel-contact-card"

    Card layout — RPG message box / status screen feel:
    - Intro text: "Let's build something together!" or similar — pixel-body-text, centered
    - Contact items in a vertical stack (className="pixel-contact-list"):

    1. Email item:
       - Icon: pixel-art envelope character (use unicode ✉ or a small pixel SVG inline)
       - Label: "EMAIL" — pixel-heading, 0.5rem
       - Value: "todyprasetya@gmail.com" — wrapped in an anchor tag with href="mailto:todyprasetya@gmail.com", className="pixel-contact-link"
       - The link should be styled as a pixel-btn (existing class)

    2. Phone item:
       - Icon: pixel-art phone character (use unicode ☎ or similar)
       - Label: "PHONE" — pixel-heading, 0.5rem
       - Value: "08118422002" — wrapped in an anchor tag with href="tel:08118422002", className="pixel-contact-link"

    3. Location item:
       - Icon: pixel-art map pin (use unicode ◆ or similar)
       - Label: "LOCATION" — pixel-heading, 0.5rem
       - Value: "Kelapa Gading, North Jakarta" — pixel-body-text (no link, just text)

    Each contact item: div with className="pixel-contact-item" containing icon + label + value.
    On desktop (768px+), lay out the 3 items in a horizontal row using flex.
    On mobile, stack vertically.

    Add a footer line: small text "© 2026 Tody Prasetya" in --pixel-text-secondary, centered, with pixel-body-text at 0.5rem.

    Use existing utility classes: pixel-panel, pixel-border, pixel-heading, pixel-body-text, pixel-btn.
    Use inline styles for layout specifics following HeroSection pattern.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - components/ContactSection.tsx exists and exports default component
    - Email displayed with mailto: link to todyprasetya@gmail.com
    - Phone displayed with tel: link to 08118422002
    - Location displayed: Kelapa Gading, North Jakarta
    - Section has id="contact"
    - Links are styled as pixel-btn elements
    - Footer with copyright text
    - npm run build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add contact CSS classes to globals.css</name>
  <files>app/globals.css</files>
  <action>
    Append a labeled CSS section to the END of app/globals.css. Follow the existing comment pattern: /* ── Contact Section ── */.

    Classes to add:
    - .pixel-contact: section wrapper, padding-top 2rem, margin-bottom 2rem
    - .pixel-contact-card: text-align center
    - .pixel-contact-intro: pixel-body-text, font-size 0.625rem, color --pixel-text-secondary, margin-bottom 1.5rem
    - .pixel-contact-list: display flex, flex-direction column, gap 1rem, align-items center. At 768px+: flex-direction row, justify-content center, gap 2rem
    - .pixel-contact-item: display flex, flex-direction column, align-items center, gap 0.5rem, padding 1rem, background --pixel-accent, border 2px solid --pixel-border, box-shadow 3px 3px 0px rgba(0,0,0,0.5), min-width 180px
    - .pixel-contact-icon: font-size 1.5rem, color --pixel-highlight
    - .pixel-contact-label: pixel-heading font, font-size 0.5rem, color --pixel-text-secondary, text-transform uppercase
    - .pixel-contact-link: pixel-body-text, font-size 0.625rem, color --pixel-highlight, text-decoration none, border-bottom 1px dashed --pixel-highlight. On hover: color --pixel-text-primary, border-bottom-color --pixel-border
    - .pixel-contact-value: pixel-body-text, font-size 0.625rem, color --pixel-text-primary
    - .pixel-contact-footer: margin-top 2rem, padding-top 1rem, border-top 1px solid --pixel-border, pixel-body-text, font-size 0.5rem, color --pixel-text-secondary
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - globals.css contains labeled /* ── Contact Section ── */ section
    - All contact CSS classes defined
    - Contact items stack on mobile, row on desktop
    - Links have hover states
    - npm run build succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contact data → component | Static embedded data, no external input |
| mailto:/tel: links → OS handler | Links open native OS handlers, no data sent to third parties |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Spoofing | Contact links | low | accept | Links are hardcoded to known-good mailto/tel values. No form submission or data collection. |
| T-03-05 | Information Disclosure | Email/phone display | low | accept | Contact info is intentionally public on a personal portfolio. This is the user's explicit intent. |
</threat_model>

<verification>
- npm run build succeeds with no errors
- ContactSection renders email, phone, location
- Email link has href="mailto:todyprasetya@gmail.com"
- Phone link has href="tel:08118422002"
- Section has id="contact" matching navigation link
- Links are keyboard-focusable and accessible
</verification>

<success_criteria>
- All contact info displayed correctly
- Email and phone links are functional (mailto/tel)
- Pixel-art RPG message box styling
- Responsive on mobile and desktop
- Accessible: links keyboard-navigable
</success_criteria>

<output>
Create `.planning/phases/03-content-sections/03-04-SUMMARY.md` when done
</output>
