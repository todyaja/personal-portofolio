---
phase: 03-content-sections
plan: 05
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - app/page.tsx
  - app/globals.css
autonomous: true
requirements:
  - TECH-03
  - TECH-04
  - AST-02
  - AST-03

estimate:
  tokens: 25000
  raw_tokens: 17000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - All 4 section components render on the home page in correct order
    - Profile picture displays correctly (already via PixelAvatar in HeroSection)
    - Personal branding logo (tody_logo_bg_removed.png) is integrated somewhere visible
    - All tech logos and Binus logo render without broken images
    - Full build passes with all sections included
  artifacts:
    - app/page.tsx (updated)
  key_links:
    - All section IDs match navigation anchor links
    - All asset paths resolve to existing files in /public/assets/
---

<objective>
Wire all content sections into the home page, integrate remaining assets (branding logo), and verify the complete Phase 3 output.

Purpose: Plans 01-04 build individual section components in isolation. This plan composes them into the full page, integrates the branding logo asset, and verifies all assets render correctly end-to-end.
Output: Updated page.tsx with all sections, branding logo integration, final verification
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/page.tsx
@app/globals.css
@components/HeroSection.tsx
@components/ExperienceTimeline.tsx
@components/SkillsSection.tsx
@components/EducationSection.tsx
@components/ContactSection.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Update page.tsx to render all sections and integrate branding logo</name>
  <files>app/page.tsx</files>
  <action>
    Update app/page.tsx to import and render all section components in order.

    Current page.tsx only renders HeroSection. Replace with:

    Import all 5 section components:
    - HeroSection from @/components/HeroSection (already imported)
    - ExperienceTimeline from @/components/ExperienceTimeline
    - SkillsSection from @/components/SkillsSection
    - EducationSection from @/components/EducationSection
    - ContactSection from @/components/ContactSection

    Render order inside the default Home function:
    1. HeroSection (already there)
    2. ExperienceTimeline
    3. SkillsSection
    4. EducationSection
    5. ContactSection

    Each component rendered as a direct child, no wrapper div needed (each section handles its own spacing via CSS).

    Additionally, integrate the personal branding logo (AST-03):
    - Add a small branding element in the HeroSection area or as a subtle footer element
    - Use next/image with src="/assets/branding/tody_logo_bg_removed.png", alt="Tody Prasetya logo"
    - Size: small, maybe 48x48px or 64x64px
    - Place it either: (a) in the HeroSection next to the name, or (b) as a small logo in the contact/footer area
    - Best option: add it to the ContactSection footer area as a branding mark, OR add it directly in page.tsx between HeroSection and ExperienceTimeline as a small divider/branding element
    - Simplest approach: render it in page.tsx as a small centered image between sections, or integrate into the existing HeroSection by modifying page.tsx to wrap HeroSection + logo together

    Actually, the cleanest approach: render the branding logo as a small element in the page layout — perhaps centered below the hero section as a visual divider/brand mark. Use inline styles: width 48px, height 48px, image-rendering pixelated, margin 1rem auto, display block, opacity 0.8.

    Also verify AST-02: The profile picture is already used by PixelAvatar (Phase 2) at /assets/profile/pfp.jpeg. Confirm this path is correct and the file exists.

  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - app/page.tsx imports and renders all 5 section components
    - Sections render in order: Hero, Experience, Skills, Education, Contact
    - Branding logo (tody_logo_bg_removed.png) rendered somewhere visible
    - Profile picture path /assets/profile/pfp.jpeg confirmed working
    - npm run build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Final CSS cleanup and full-page verification</name>
  <files>app/globals.css</files>
  <action>
    Perform final CSS cleanup and add any cross-section styling needed.

    1. Add a section divider utility if not already present:
       - .pixel-section-divider: height 2px, background repeating-linear-gradient(90deg, var(--pixel-border) 0px, var(--pixel-border) 4px, transparent 4px, transparent 8px), margin 1rem 0. This creates a pixel-art dashed divider between sections.

    2. Add a global section spacing rule if needed:
       - Ensure each section (pixel-experience, pixel-skills, pixel-education, pixel-contact) has consistent vertical spacing. Check that padding-top values are consistent across all section components.

    3. Add branding logo CSS:
       - .pixel-brand-logo: display block, margin 1.5rem auto, width 48px, height 48px, image-rendering pixelated, opacity 0.8. On hover: opacity 1, transform scale(1.05).

    4. Verify all asset paths used across components resolve to existing files:
       - /assets/profile/pfp.jpeg (AST-02) — used by PixelAvatar
       - /assets/branding/tody_logo_bg_removed.png (AST-03) — used in page.tsx
       - /assets/logos/binus_univ.png (AST-04) — used by EducationSection
       - /assets/tech/java.png, kotlin.png, net.png, oracle.png, postgre.png, reactjs.png, php.png (AST-05) — used by SkillsSection and ExperienceTimeline

    5. Run npm run build and verify:
       - All pages generate successfully
       - No missing image warnings
       - All section IDs present in output HTML

  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - globals.css has section divider utility
    - Branding logo CSS defined
    - All asset paths verified to resolve to existing files
    - npm run build succeeds with all sections
    - No missing image warnings in build output
    - All section IDs (hero, experience, skills, contact) present in generated HTML
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                |
| ------------------------------ | ---------------------------------------------------------- |
| Asset paths → public directory | Static file references, no dynamic loading                 |
| Component composition → page   | All components are local imports, no external dependencies |

## STRIDE Threat Register

| Threat ID | Category  | Component              | Severity | Disposition | Mitigation Plan                                                                                          |
| --------- | --------- | ---------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------- |
| T-03-06   | Tampering | Asset paths            | low      | accept      | All asset paths are hardcoded to local /public/assets/ files. No external URLs or dynamic asset loading. |
| T-03-SC   | Tampering | npm/pip/cargo installs | low      | accept      | No new package installs in this plan. All dependencies from Phase 1.                                     |

</threat_model>

<verification>
- npm run build succeeds with all 5 sections rendering
- All section IDs present: hero, experience, skills, contact
- All asset images load without 404 errors
- Branding logo visible on page
- Page layout is coherent — sections flow naturally
- Navigation anchor links work: /#experience, /#skills, /#contact all scroll to correct sections
</verification>

<success_criteria>

- All 5 sections render on the home page in correct order
- All assets (profile pic, branding logo, Binus logo, tech logos) display correctly
- Page builds without errors or missing image warnings
- Navigation anchors work end-to-end
- Complete Phase 3 output is functional and visually cohesive
  </success_criteria>

<output>
Create `.planning/phases/03-content-sections/03-05-SUMMARY.md` when done
</output>
