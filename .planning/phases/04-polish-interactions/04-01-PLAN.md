---
phase: 04-polish-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - components/LoadingSpinner.tsx
autonomous: true
requirements:
  - VIS-05
  - TECH-05

estimate:
  tokens: 40000
  raw_tokens: 25000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - Full-screen dark overlay with centered RPG-style spinner appears on initial page load
    - Spinner displays for a minimum of 1 second even if assets load faster
    - Spinner fades out over 0.3s after minimum display time elapses
    - Spinner does not reappear on client-side navigation
  artifacts:
    - components/LoadingSpinner.tsx exists as a client component
    - CSS keyframes for coin bounce, dot animation, and fade-out in globals.css
  key_links:
    - LoadingSpinner mounted inside layout.tsx body so it overlays all content
    - useState + useEffect timing logic coordinates min display with fade-out
---

<objective>
Create an RPG-styled loading spinner that displays on initial page load with a bouncing pixel coin/gem icon and animated "LOADING..." text, then fades out after a minimum 1-second display time.

Purpose: First impression matters — the loading spinner sets the retro RPG tone immediately and masks asset loading time.
Output: LoadingSpinner client component with CSS animations, mounted in root layout.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/globals.css
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoadingSpinner component with RPG coin animation</name>
  <files>components/LoadingSpinner.tsx, app/globals.css</files>
  <action>
    Create `components/LoadingSpinner.tsx` as a `"use client"` component. The component renders a full-screen fixed overlay (z-index below pixel-scanline's 9999, use 9998) with dark semi-transparent background (`rgba(26, 26, 46, 0.95)` matching `--pixel-bg-dark`). Center content vertically and horizontally using flexbox.

    The spinner icon is a pixel-art coin/gem built with pure CSS — a 32x32px square with `--pixel-highlight` (#ffd700) background, a 2px solid `--pixel-border` border, and a box-shadow for pixel depth. Animate it with a `pixel-coin-bounce` keyframe: translateY bouncing between 0 and -12px in a 0.6s ease-in-out infinite loop.

    Below the coin, render "LOADING..." text using `--font-pixel-heading` at 0.625rem, colored `--pixel-text-primary`. The three dots animate via a `pixel-loading-dots` keyframe that cycles opacity on a `<span>` wrapping "..." — or use three separate `<span>` elements each with staggered animation-delay (0s, 0.2s, 0.4s) toggling opacity between 0.2 and 1.

    Add CSS to `app/globals.css` in a new `/* -- Loading Spinner -- */` section: keyframes `pixel-coin-bounce`, `pixel-loading-dots`, and utility classes `.pixel-loading-overlay`, `.pixel-loading-coin`, `.pixel-loading-text`, `.pixel-loading-dot`. Also add a `.pixel-loading-overlay.fade-out` class with `opacity: 0; transition: opacity 0.3s ease;` and `pointer-events: none`.

    Respect `prefers-reduced-motion`: inside `@media (prefers-reduced-motion: reduce)`, disable the bounce and dot animations (set `animation: none`), show static coin and static "LOADING..." text.
  </action>
  <verify>
    <automated>npm run build succeeds with no TypeScript errors</automated>
  </verify>
  <done>
    LoadingSpinner component renders a full-screen dark overlay with a bouncing pixel coin and animated "LOADING..." dots. All animation keyframes are in globals.css. Component accepts no props.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount LoadingSpinner in layout with show/hide timing logic</name>
  <files>app/layout.tsx, components/LoadingSpinner.tsx</files>
  <action>
    Modify `components/LoadingSpinner.tsx` to add show/hide timing logic. Use `useState` for two booleans: `visible` (initially `true`) and `fading` (initially `false`). In `useEffect`, set a timeout for 1000ms (minimum display time). After 1000ms, set `fading` to `true` (triggers CSS fade-out class). After an additional 300ms (matching the CSS transition duration), set `visible` to `false` to unmount the component entirely.

    The component returns `null` when `visible` is `false`. When rendering, apply class `pixel-loading-overlay` always, plus `fade-out` when `fading` is `true`.

    Modify `app/layout.tsx`: import `LoadingSpinner` and render it as the first child inside `<body>`, before the `<div className="pixel-container">`. This ensures the overlay covers the full viewport including the navigation area. The component tree becomes: `body > LoadingSpinner + div.pixel-container > Navigation + children + pixel-scanline`.

    The LoadingSpinner only shows on initial page load — since it's a client component with state that resolves after mount, client-side navigation between pages will not re-trigger it (Next.js App Router preserves the layout shell).
  </action>
  <verify>
    <automated>npm run build succeeds; manually verify in dev server that spinner appears on fresh load and fades out after ~1.3s</automated>
  </verify>
  <done>
    On fresh page load, the loading spinner overlay appears immediately, displays for at least 1 second, then fades out over 0.3s and unmounts. On subsequent client-side navigation, the spinner does not reappear.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No external inputs or API calls. All rendering is client-side only.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service | LoadingSpinner | low | accept | Component unmounts after fade-out; no persistent resource usage. If animation loops infinitely due to bug, it's a cosmetic issue only. |
</threat_model>

<verification>
1. `npm run build` completes without errors
2. Fresh page load shows full-screen dark overlay with bouncing pixel coin and animated "LOADING..." text
3. Spinner remains visible for at least 1 second
4. After minimum time, overlay fades out over ~0.3s and disappears
5. Client-side navigation (clicking nav links) does not re-trigger the spinner
6. With `prefers-reduced-motion: reduce` enabled in browser dev tools, coin does not bounce and dots do not animate — static display only
</verification>

<success_criteria>
- Loading spinner displays for minimum 1s on initial page load
- Loading spinner shows bouncing pixel coin/gem with "LOADING..." text with animated dots
- Loading spinner fades out quickly (0.3s) after minimum display time
- Spinner does not reappear on client-side navigation
- All animations respect prefers-reduced-motion preference
</success_criteria>

<output>
Create `.planning/phases/04-polish-interactions/04-01-SUMMARY.md` when done
</output>
