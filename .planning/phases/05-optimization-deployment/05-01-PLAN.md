---
phase: 05-optimization-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - components/HeroSection.tsx
  - components/PixelAvatar.tsx
  - components/SkillsSection.tsx
  - components/EducationSection.tsx
  - components/ContactSection.tsx
  - components/LoadingSpinner.tsx
  - public/assets/**/*
autonomous: true
requirements:
  - TECH-05

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - All images optimized and served in modern formats (WebP/AVIF)
    - Below-fold components lazy-loaded
    - Bundle size minimized with code splitting
    - Lighthouse performance score > 90
  artifacts:
    - next.config.ts (updated with image optimization config)
    - Optimized images in public/assets/
  key_links:
    - next/image component usage across all components
    - Dynamic imports for below-fold sections
---

<objective>
Optimize performance to achieve < 3s load time on 3G and Lighthouse score > 90.

Purpose: Ensure fast load times for all users, especially on slower connections.
Output: Optimized images, lazy-loaded components, minimized bundle size.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@D:/Code/personal-web-portofolio-v2/.planning/PROJECT.md
@D:/Code/personal-web-portofolio-v2/.planning/ROADMAP.md
@D:/Code/personal-web-portofolio-v2/next.config.ts
@D:/Code/personal-web-portofolio-v2/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize images with next/image and compression</name>
  <files>
    next.config.ts,
    components/HeroSection.tsx,
    components/PixelAvatar.tsx,
    components/SkillsSection.tsx,
    components/EducationSection.tsx,
    components/ContactSection.tsx
  </files>
  <action>
    Configure next.config.ts to enable next/image optimization with WebP/AVIF output formats. Update all components to use next/image instead of img tags or plain Image imports. Set appropriate width/height, priority for above-fold images (hero avatar), and loading="lazy" for below-fold images. Compress source images in public/assets/ using squoosh or sharp to reduce file sizes (target: < 50KB per image where possible). Add blurDataURL placeholders for progressive loading.
  </action>
  <verify>
    <automated>npm run build && npm run start, then verify images load in WebP/AVIF format via browser dev tools network tab</automated>
  </verify>
  <done>
    All images use next/image component with proper sizing and optimization. Source images compressed. Lighthouse image optimization checks pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement lazy loading for below-fold components</name>
  <files>app/page.tsx</files>
  <action>
    Use next/dynamic to lazy-load ExperienceTimeline, SkillsSection, EducationSection, and ContactSection components. Keep HeroSection eagerly loaded (above-fold). Add loading.tsx fallback with pixel-art styled skeleton for each lazy-loaded section. Set ssr: true for SEO but ensure client-side hydration is deferred until needed.
  </action>
  <verify>
    <automated>npm run build && check bundle analyzer output shows code splitting. Verify initial HTML doesn't include below-fold component code.</automated>
  </verify>
  <done>
    Below-fold sections are dynamically imported with loading fallbacks. Initial bundle size reduced. Page still renders correctly with SSR.
  </done>
</task>

<task type="auto">
  <name>Task 3: Minimize bundle size and optimize fonts</name>
  <files>app/layout.tsx, next.config.ts</files>
  <action>
    Enable Next.js bundle analyzer in dev mode to identify large dependencies. Configure font optimization via next/font/google (already using Press_Start_2P and VT323 - ensure display: swap is set). Remove unused CSS with Tailwind's purge (already configured in v4). Enable React strict mode for better performance. Configure next.config.ts with optimizePackageImports for heavy packages if any. Add compression: true for gzip/brotli.
  </action>
  <verify>
    <automated>npm run build && verify bundle sizes in build output. Run Lighthouse performance audit and confirm score > 90.</automated>
  </verify>
  <done>
    Bundle size minimized. Fonts load with display: swap. Lighthouse performance score > 90. First Contentful Paint < 1.5s, Largest Contentful Paint < 2.5s.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Image uploads | Public assets served to clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | Image assets | low | accept | Static assets in public/ are version-controlled, no runtime upload |
| T-05-02 | Denial of Service | Large images | low | mitigate | Image optimization via next/image prevents oversized payloads |
</threat_model>

<verification>
- npm run build completes without errors
- npm run start serves optimized images
- Lighthouse performance score > 90
- All images load in WebP/AVIF format
- Below-fold components lazy-loaded (verify via bundle analyzer)
- First Contentful Paint < 1.5s
- Largest Contentful Paint < 2.5s
- Total bundle size < 200KB (gzipped)
</verification>

<success_criteria>
- Lighthouse performance score > 90
- Site loads in < 3s on 3G simulation
- All images optimized and served in modern formats
- Below-fold components lazy-loaded
- Bundle size minimized with code splitting
</success_criteria>

<output>
Create `.planning/phases/05-optimization-deployment/05-01-SUMMARY.md` when done
</output>
