---
phase: 05-optimization-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Navigation.tsx
  - components/HeroSection.tsx
  - components/ExperienceTimeline.tsx
  - components/SkillsSection.tsx
  - components/EducationSection.tsx
  - components/ContactSection.tsx
  - components/PixelAvatar.tsx
  - components/LoadingSpinner.tsx
  - components/TypeWriter.tsx
  - components/ScrollReveal.tsx
  - app/globals.css
autonomous: true
requirements:
  - TECH-06

estimate:
  tokens: 40000
  raw_tokens: 25000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - All interactive elements keyboard accessible
    - Proper ARIA labels on all interactive components
    - Color contrast ratios meet WCAG 2.1 AA (4.5:1 for text)
    - Screen reader compatible with semantic HTML
    - Focus indicators visible and logical
  artifacts:
    - Updated components with ARIA attributes
    - globals.css with focus-visible styles
  key_links:
    - Keyboard navigation flow through all sections
    - Screen reader announcements for dynamic content
---

<objective>
Ensure full accessibility compliance with WCAG 2.1 AA standards.

Purpose: Make the portfolio accessible to all users including those using assistive technologies.
Output: Keyboard navigation, ARIA labels, proper contrast, screen reader compatibility.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@D:/Code/personal-web-portofolio-v2/.planning/PROJECT.md
@D:/Code/personal-web-portofolio-v2/.planning/ROADMAP.md
@D:/Code/personal-web-portofolio-v2/components/Navigation.tsx
@D:/Code/personal-web-portofolio-v2/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix keyboard navigation</name>
  <files>
    components/Navigation.tsx,
    components/HeroSection.tsx,
    components/ExperienceTimeline.tsx,
    components/SkillsSection.tsx,
    components/EducationSection.tsx,
    components/ContactSection.tsx,
    app/globals.css
  </files>
  <action>
    Test keyboard navigation through all interactive elements (Tab, Shift+Tab, Enter, Space, Escape). Ensure logical tab order following visual layout. Add visible focus indicators (focus-visible) with pixel-art styling (dashed borders, highlight colors). Ensure all buttons and links are focusable. Add skip-to-content link at top of page. Verify mobile menu keyboard interaction (Escape to close, focus trap when open). Ensure all interactive elements have proper role attributes.
  </action>
  <verify>
    <automated>Manual keyboard navigation test: Tab through entire page, verify focus visible on all interactive elements. Check mobile menu: Tab to hamburger, Enter to open, Tab through menu items, Escape to close.</automated>
  </verify>
  <done>
    All interactive elements keyboard accessible. Focus indicators visible with pixel styling. Skip-to-content link works. Mobile menu keyboard interaction complete. Tab order logical and follows visual layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ARIA labels and semantic HTML</name>
  <files>
    components/HeroSection.tsx,
    components/ExperienceTimeline.tsx,
    components/SkillsSection.tsx,
    components/EducationSection.tsx,
    components/ContactSection.tsx,
    components/PixelAvatar.tsx,
    components/LoadingSpinner.tsx,
    components/TypeWriter.tsx,
    components/ScrollReveal.tsx
  </files>
  <action>
    Add aria-label to all sections (aria-label="About", aria-label="Experience", etc.). Ensure all images have alt text (decorative images get alt="", informative images get descriptive alt). Add aria-live="polite" to TypeWriter component for screen reader announcements. Add role="img" and aria-label to PixelAvatar. Ensure LoadingSpinner has aria-hidden="true" or role="status" with aria-label. Add aria-current="true" to active nav link (already present, verify). Ensure all form fields in ContactSection have associated labels. Use semantic HTML (section, article, nav, main, header, footer) appropriately.
  </action>
  <verify>
    <automated>Run axe-core automated accessibility audit via browser extension or npm package. Verify no critical or serious violations.</automated>
  </verify>
  <done>
    All sections have aria-labels. All images have appropriate alt text. Dynamic content announced to screen readers. Semantic HTML structure correct. axe-core audit passes with no critical violations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify color contrast and screen reader compatibility</name>
  <files>app/globals.css</files>
  <action>
    Test color contrast ratios using WebAIM contrast checker. Ensure all text meets WCAG 2.1 AA (4.5:1 for normal text, 3:1 for large text). Adjust pixel color variables if needed (--pixel-text-primary, --pixel-text-secondary, --pixel-highlight, --pixel-accent). Test with screen reader (NVDA on Windows, VoiceOver on Mac) to verify content is announced correctly. Ensure decorative elements (scanline effect, pixel borders) are hidden from screen readers (aria-hidden="true"). Verify heading hierarchy (h1 → h2 → h3, no skipping).
  </action>
  <verify>
    <automated>Run WAVE accessibility evaluation tool or axe DevTools. Verify color contrast ratios meet 4.5:1 for all text. Test with screen reader: navigate through all sections and verify content is announced correctly.</automated>
  </verify>
  <done>
    All color contrast ratios meet WCAG 2.1 AA. Screen reader announces all content correctly. Decorative elements hidden from assistive tech. Heading hierarchy correct. Lighthouse accessibility score > 90.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User interface | Accessibility affects all users |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Denial of Service | Accessibility barriers | high | mitigate | WCAG 2.1 AA compliance ensures access for users with disabilities |
</threat_model>

<verification>
- Keyboard navigation works through all interactive elements
- Focus indicators visible and styled with pixel theme
- Skip-to-content link functional
- All images have appropriate alt text
- ARIA labels present on all sections
- Color contrast ratios meet WCAG 2.1 AA (4.5:1)
- Screen reader compatible (test with NVDA/VoiceOver)
- Heading hierarchy correct (h1 → h2 → h3)
- axe-core audit passes with no critical violations
- Lighthouse accessibility score > 90
</verification>

<success_criteria>
- Lighthouse accessibility score > 90
- All interactive elements keyboard accessible
- Proper ARIA labels on all components
- Color contrast meets WCAG 2.1 AA
- Screen reader compatible
- Focus indicators visible and logical
</success_criteria>

<output>
Create `.planning/phases/05-optimization-deployment/05-02-SUMMARY.md` when done
</output>
