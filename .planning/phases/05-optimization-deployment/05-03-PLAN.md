---
phase: 05-optimization-deployment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/sitemap.ts
  - app/robots.ts
  - public/robots.txt
  - public/sitemap.xml
  - next.config.ts
autonomous: true
requirements:
  - TECH-05

estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - Meta tags present for all pages (title, description, keywords)
    - Open Graph tags for social sharing
    - Twitter Card tags configured
    - Sitemap.xml generated and accessible
    - Robots.txt configured correctly
    - Structured data (JSON-LD) for person/portfolio
  artifacts:
    - app/sitemap.ts (dynamic sitemap generator)
    - app/robots.ts (robots.txt generator)
    - Updated app/layout.tsx with comprehensive metadata
  key_links:
    - Metadata export in layout.tsx
    - Sitemap accessible at /sitemap.xml
    - Robots.txt accessible at /robots.txt
---

<objective>
Implement comprehensive SEO setup for search engine visibility and social sharing.

Purpose: Ensure the portfolio ranks well in search results and displays correctly when shared on social media.
Output: Meta tags, Open Graph, Twitter Cards, sitemap.xml, robots.txt, structured data.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@D:/Code/personal-web-portofolio-v2/.planning/PROJECT.md
@D:/Code/personal-web-portofolio-v2/.planning/ROADMAP.md
@D:/Code/personal-web-portofolio-v2/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure comprehensive metadata and structured data</name>
  <files>app/layout.tsx</files>
  <action>
    Expand the existing metadata export in layout.tsx to include: keywords array (Backend Engineer, Full Stack Developer, Go, JavaScript, Java, TypeScript, PostgreSQL, Portfolio), authors, publisher, creator. Add comprehensive Open Graph tags: images (use /assets/branding/tody_logo_bg_removed.png), url, siteName. Add Twitter Card configuration with creator and site handles if available. Add JSON-LD structured data for Person schema (name, jobTitle, worksFor, knowsAbout, sameAs for social links). Add canonical URL. Configure viewport for mobile. Add theme-color meta tag matching pixel theme.
  </action>
  <verify>
    <automated>npm run build && npm run start. Visit page and inspect meta tags in browser dev tools. Test with Facebook Sharing Debugger and Twitter Card Validator.</automated>
  </verify>
  <done>
    All meta tags present and correct. Open Graph displays correctly when shared on Facebook/LinkedIn. Twitter Card displays correctly. JSON-LD structured data validates via Google Rich Results Test. Canonical URL set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate sitemap.xml and robots.txt</name>
  <files>app/sitemap.ts, app/robots.ts</files>
  <action>
    Create app/sitemap.ts using Next.js Metadata API to generate sitemap.xml dynamically. Include homepage URL with priority 1.0 and changefreq weekly. Add lastModified dates. Create app/robots.ts to generate robots.txt with User-agent: *, Allow: /, Sitemap: [site-url]/sitemap.xml. Configure next.config.ts with trailingSlash: false for clean URLs. Add metadataBase to layout.tsx for absolute URL generation.
  </action>
  <verify>
    <automated>npm run build && npm run start. Visit /sitemap.xml and /robots.txt to verify they are generated correctly. Validate sitemap with Google Search Console sitemap testing tool.</automated>
  </verify>
  <done>
    sitemap.xml accessible at /sitemap.xml with correct URLs and metadata. robots.txt accessible at /robots.txt with correct directives. Both files dynamically generated by Next.js.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public metadata | SEO metadata visible to all |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Metadata | low | accept | Public portfolio, all metadata is intended to be public |
</threat_model>

<verification>
- Meta tags present and correct (title, description, keywords, author)
- Open Graph tags display correctly on social platforms
- Twitter Card displays correctly
- JSON-LD structured data validates
- sitemap.xml accessible and valid
- robots.txt accessible and correct
- Canonical URL set
- Lighthouse SEO score > 90
</verification>

<success_criteria>
- Lighthouse SEO score > 90
- All meta tags present and correct
- Open Graph and Twitter Cards configured
- sitemap.xml generated and accessible
- robots.txt configured correctly
- Structured data validates
</success_criteria>

<output>
Create `.planning/phases/05-optimization-deployment/05-03-SUMMARY.md` when done
</output>
